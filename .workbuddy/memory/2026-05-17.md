# 2026-05-17 工作记录

## 食谱 App 接入 DeepSeek + GitHub Pages 部署改造

- 将 `src/services/api.ts` 完全重写：DeepSeek API 直连（fetch），localStorage 替代后端数据库
- 修改 `vite.config.ts`：添加 `base: '/frontend/'` 适配 GitHub Pages 子路径
- 创建 `.env.example`，更新 `.gitignore` 保护 API Key
- 创建 `.github/workflows/deploy.yml`：GitHub Actions 自动构建部署
- 修复 TS 编译报错（IngredientCard、RecipeCard、ScanReceipt、MyIngredients）
- 本地构建验证通过（77 modules, 1.77s）
- **仓库名为 `frontend`**，GitHub Pages URL 为 `https://<username>.github.io/frontend/`
- 用户需要：① 本地创建 `.env` 填入 DeepSeek Key ② Push 代码 ③ GitHub 仓库设置 Pages Source 为 GitHub Actions
